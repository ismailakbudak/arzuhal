<%= form_for([@user, @user.tweets.build]) do |f| %>
  
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@tweet.errors.count, "error") %> prohibited this user from being saved:</h4>

      <ul>
      <% @tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :tweet %><br>
    <%= f.text_area :tweet %>
  </p> 
  <p>
    <%= f.submit %>
  </p>
<% end %> 
